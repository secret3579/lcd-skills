# LCD Skills

When the user requests anything related to the LCD (find, pair, notify, show, ping, display, screen), read `SKILL.md` and follow the instructions.

Device config is stored at `~/.config/autonomous-lcd.json`.
